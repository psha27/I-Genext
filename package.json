{
  "name": "igenext-web",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace client",
    "server": "npm run dev --workspace server",
    "test": "node --test server/test/*.test.js scripts/experience.test.mjs scripts/proxy-origin.test.mjs",
    "admin:setup": "node server/scripts/setup-admin.mjs",
    "start": "npm run start --workspace server"
  }
}
